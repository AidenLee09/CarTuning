VERTEX_PROJECT_ID=your-gcp-project-id
VERTEX_LOCATION=us-central1
VERTEX_MODEL=gemini-3-flash
VERTEX_API_KEY=your-server-side-api-key

# Optional: use a custom Vertex-compatible endpoint instead of the default URL.
# VERTEX_ENDPOINT=https://your-api.example.com/generateContent
